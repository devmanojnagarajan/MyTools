<Window x:Class="MyTools.CategorySelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Categories"
        Height="450" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <TextBox x:Name="SearchBox"
                 Grid.Row="0"
                 Margin="0,0,0,10"
                 Padding="5"
                 Text=""
                 ToolTip="Search categories"
                 TextChanged="SearchBox_TextChanged"/>

        <!-- Category List -->
        <ListBox x:Name="CategoryListBox"
                 Grid.Row="1"
                 SelectionMode="Multiple"
                 Margin="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}"
                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                              Margin="2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Select All" Width="75" Margin="0,0,10,0" Click="SelectAll_Click"/>
            <Button Content="Clear All" Width="75" Margin="0,0,10,0" Click="ClearAll_Click"/>
            <Button Content="OK" Width="75" Margin="0,0,10,0" Click="OK_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
